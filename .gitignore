node_modules
dist
dist-ssr
*.local
.env
.env.*
# Keep the committed template (it contains no secrets)
!.env.example
.DS_Store
.vscode/*
!.vscode/extensions.json

# Vercel
.vercel

# Test / coverage artifacts
coverage

# Recognition benchmark fixtures — real scan photos + Scryfall reference
# images, never committed (copyrighted card art). See benchmark/README.md.
benchmark/fixtures/*
!benchmark/fixtures/manifest.example.json
